import { Product } from './product'
import { ProductSet } from './product-set'
import { ProductSetOption } from './product-set-option'
import { ProductOption } from './product-option'
import { ProductOptionValue } from './product-option-value'
import { ProductBatch } from './product-batch'
import { Lot } from './lot'

export const entities = [Product, ProductSet, ProductSetOption, ProductOption, ProductOptionValue, ProductBatch, Lot]

export { Product, ProductSet, ProductSetOption, ProductOption, ProductOptionValue, ProductBatch, Lot }
